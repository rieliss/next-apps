generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mongodb"
  url      = env("MONGO_URL")
}

model details {
  id          String    @id @default(auto()) @map("_id") @db.ObjectId
  description String
  status      Boolean @default(false)
  title       String
}
