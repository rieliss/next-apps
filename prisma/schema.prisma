
datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model More {
  id          Int    @id @default(autoincrement())
  description String
  status      Boolean @default(false)
  title       String
}
